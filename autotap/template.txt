class ${class_name} < Formula
  desc '${desc}'
  homepage '${homepage}'
  version '${version}'

  url '${url}'
  sha256 '${sha256}'

  depends_on :arch => :x86_64

  def install
    bin.install Dir['*']
  end
end
